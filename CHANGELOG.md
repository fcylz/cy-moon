# 更新记录（CHANGELOG）

本文件跟 `app.js` 顶部的 `APP_VERSION` / `APP_CHANGELOG` 一一对应。
App 内也可以直接看：**数据 → 关于 · 版本**（手机上确认"壳子有没有加载到新构建"就靠它）。

## 版本号规则

`MAJOR.MINOR.PATCH`
- **MINOR**：加功能、改交互、改数据规格
- **PATCH**：只修 bug，不动数据结构

发版流程：改 `APP_VERSION` / `APP_BUILD` → 在 `APP_CHANGELOG` 顶部插一条 → 在本文件加一节 → commit。
**数据规格变更必须写明**（新增/删除字段、迁移函数名），否则另一台设备拉到旧数据会出错。

---

## v1.15.4 · 2026-09-25

**主题：已经存在的重复内容，怎么清掉**

前两版修的都是「**别再产生新的重复**」，这一版回答的是「**库里已经重复的那批怎么办**」。

### 两种清理途径（同一套判重键，幂等，效果完全一样）

**① 自动 —— 每次启动 1.5 秒后跑一次 `dedupeLib(true)`**

- 没清掉任何东西时**完全静默**，连写盘都不发生；
- 清掉了才 toast 一句：`已自动清理重复内容：字卡 N 条 · 表情 M 个`。

**② 手动 —— 设置 → 数据 → 存储管理 → 清理重复内容**

先弹确认框告诉你**检测到多少条**、**判重标准是什么**，确认后才动数据：

```
检测到重复内容：字卡 12 条 · 表情 5 个。

判重标准：字卡 = 分类 + 正文；表情 = 图片路径（raw / jsdelivr 两种域名算同一张）。
清理后每种只留一条，表情优先保留打得开的那条。

确定清理吗？
```

不想清就取消 —— 什么都不会发生。

### 清理规则

- 每种内容只留**一条**；
- 表情优先保留打得开的那条（`_stickerRank`：raw.githubusercontent.com 本机不可达 → 让位给 jsdelivr）；
- ⛔ 只删**内容键完全相同**的冗余项。空正文、同正文不同分类、base64 上传件都不参与判重，
  绝不会删掉任何"看起来像但其实不同"的内容。

### 清理会不会让聊天里的表情失效

**不会 —— 但这是专门处理过的，不是碰巧。**

聊天消息里存的是 `stickerId`（引用锚点，不是图片本体）。如果去重时只把多余那条删掉，
老消息就再也找不到它 → 那批表情**永久退化成占位图**，
正是 v1.14.x「顺手删了引用锚点」那个坑的翻版（`painterSeed` / `imgId` / `stickerId` 同一个教训）。

所以去重改成 `_dedupeStickersWithMap()`：除了返回清理后的列表，还返回
「**被合并掉 → 保留下来**」的 id 映射，再由 `_remapStickerIds()` 把引用改指过去。
两者是同一张图，所以**聊天里显示的表情完全不变**。

覆盖的引用位置：

| 位置 | 处理 |
| --- | --- |
| 聊天消息 `m.stickerId` | 改指 |
| 引用块 `m.quote.stickerId` | 改指 |
| 留言板帖子 / 评论 | 改指 |
| 兜底 | 映射另存 `cfg.stickerAlias`，`_findSticker` 查不到时回溯（发送 / 屏蔽 / 缩略图缓存统一走它） |

**保留哪一条**的优先级：① 打得开的（raw 死链让位给 jsdelivr）② **有聊天在引用的**
③ 位置靠前的 —— 第 ② 条让绝大多数消息连改都不用改。

### 验证

`countDupes()` 报告的数量与 `_dedupeBy` 实际清理的数量**逐例对齐**（3 项断言通过），
不会出现"说有 5 条、实际只清了 2 条"。

| 用例 | 报告 | 实际清理 |
| --- | --- | --- |
| 字卡同文同分类重复 3 份 | 2 | 2 |
| 表情 raw + jsdelivr 同图各一份 | 1 | 1 |
| 干净数据 | 0 | 0 |

引用连续性另跑 **11 项断言全通过**：重复两张收成一张且保留打得开的那条、
3 处引用全部改指（含引用块）、消息仍指向存在的表情、保留被引用的那条时一个引用都不用改、
留言板帖子与评论都跟上、干净数据不产生映射、base64 上传件不与云端撞键。

---

## v1.15.3 · 2026-09-25

**主题：云端库「重复导入」收尾（已导入的项不再可选）；顺带修掉一个「拿渲染下标当身份」的老 bug**

### 问题一：云端表情库 / 字卡库刷新后重新导入会叠加

判重虽然生效，但**界面上看不出哪些已经导入过** —— 刷新一次列表，原来的勾选状态也没了，
再「全选」就会把导入过的重新选上，于是又进一份（用户报的"重新拉取会叠加"）。

### 修复

- 云端字卡库、云端表情库：**已导入的项标「已导入」并禁用勾选**（整项淡化），看一眼就知道哪些有了；
- 「全选」只选本地还缺的那些；分组头的全选复选框也跳过已导入的项；
- 两处导入收尾再跑一次 `_dedupeBy` 兜底 —— 即便某条判重键对不上，也不会在库里留下重复。

### 问题二：云端库的「选中项」拿**渲染下标**当身份 → 筛选后会导错

云端库的复选框 id 是**渲染时的数组下标**（字卡 `${cat}::${ii}`、表情 `csk_${i}`），
而导入时是拿这个下标去**原始数组**里取内容。两边一旦不是同一个数组，就取到别的东西：

| 库 | 渲染的数组 | 导入取的数组 | 后果 |
| --- | --- | --- | --- |
| 字卡 | `g.items` **滤掉空正文后**的子集 | `g.items` 原数组 | 空项之后的行**整体错位** → 勾第 N 行、导入的是别的行 |
| 表情 | 搜索**过滤后**的子集 | `_cskData` 全量 | **一用搜索就错** → 勾一张、导进来的是另一张 |

字卡那条是**潜在**的（云端 `Word/word.json` 实测 1521 条、3 组、空正文 0 条 → 目前不触发）；
**表情那条是活的**：只要在表情库里搜过再勾选，导进来的就不是你点的那张。

### 修复

下标一律取**原始数组里的位置**，筛选只影响显示、不影响身份：

- 字卡：`items` 先 `.map((item, oi) => ({item, oi}))` 再过滤，id 用 `oi`；
- 表情：`const oi = (window._cskData || stks).indexOf(s)`，id 用 `oi`（找不到则跳过该行）。

### 补充验证

从真实 `app.js` 切片跑 **41 项断言全通过**，重点是「筛选后勾选，导入的是不是同一项」：

- 字卡：组内含空正文项时，勾「B」导入 B、勾「C」导入 C（**含旧代码的对照断言**：旧写法会把 B 标成过滤后的位置、导错行）；
- 表情：搜索只剩 1 行时勾选，导入的正是那一张（同样含旧代码对照）；
- 不叠加：本机已有的标「已导入」+「全选」跳过 → 导入后条数不变、**再拉一次待导入为 0**；
- 回归：无筛选时下标不变、跨域名（raw ↔ jsdelivr）仍判重、无 url/无正文的项原样保留不缩水、
  base64 上传件不与云端撞键、同正文不同分类算两条。

### 版本号说明

这批修复最初被**直接追加进 v1.15.2 的段落**，但 v1.15.2 早已发布（commit `1b43acc`）。
同一个版本号对应两份不同的代码，手机上就**没法靠版本号判断修复到没到** —— 故另起 v1.15.3。
（v1.15.2 的段落已回退为它当初发布时的内容。）

---

## v1.15.2 · 2026-09-25

**主题：字卡库 / 表情包库出现重复内容；云端新增的表情包拉不到**

### 问题一：重复内容（确实是云同步造成的）

用户反馈库里出现内容相同的多条字卡/表情。两个独立来源：

**① 跨设备各导入一遍 → id 不同 → 合并不去重**

云端导入时 id 是这样生成的：

```js
cards.push({ id: "c" + Date.now() + (added++), ... });      // 字卡
stickers.push({ id: "sk" + Date.now() + added, ... });      // 表情
```

两台设备各自导入同一批云端内容，时间戳必然不同 → id 不同。
而合并层 `_mergeById` 只认 id：

```js
if(b.cards) cards = take(cards, b.cards, _mergeById);      // ← 同内容算两条
```

于是「每同步一次就多一份」。

**② 表情的 src 域名换过 → src 判重失效**

云端默认地址改过一次（`raw.githubusercontent.com` → `cdn.jsdelivr.net`），
同一张图两个地址，导入层的 `existing.has(src)` 判不出来 → 各进一份。

### 修复

- 新增 `_cardKey`（分类+正文）、`_stickerKey`（取 `/Meme/` 之后的路径，抹掉域名差异）；
- 新增 `_dedupeBy(list, keyFn, better)`：并集之后按内容键再收一遍，保留更优的一条；
- `_mergeCards` / `_mergeStickers` 取代 cards/stickers 的 `_mergeById`；
- 表情同图时优先保留**打得开**的那条（`_stickerRank`：raw.githubusercontent.com 本机不可达 → 排后面）；
- 三处导入（云端字卡 / 云端表情 / 粘贴链接）改用同一套判重键。

⛔ 只判「内容键完全相同」，不做模糊匹配 —— 宁可漏删，不可误删。空正文、同文不同分类都不参与判重。

### 清理历史遗留

新增 `dedupeLib()`，启动时自动跑一次：把已经存在的重复项收掉，
**没清掉任何东西就不写盘**，清掉了才 toast 告知。幂等，跑多少次结果一致。

### 问题二：云端新增的表情包拉不到

### 根因

索引缓存**没有过期机制**，而且刷新函数压根没接进 UI：

```js
if (cached && cached.stickers && cached.stickers.length) return cached.stickers;  // ← 永远用旧缓存
```

全项目搜 `refreshCloudStickers` / `refreshCloudCards`，只有定义、没有调用点
（音乐库有「刷新」按钮，字卡库和表情库没有）。

### 修复

- 缓存加 **6 小时 TTL**（`CLOUD_INDEX_TTL`），过期自动重拉；
- 拉取 URL 追加 `t=Date.now()`，绕过浏览器/CDN 缓存；
- 两个云端库弹窗新增「刷新云端」按钮，刷新后就地重渲染列表；
- 弹窗内补一行状态（云端 N 组 / N 个）—— 之前 `updateCloudStickerStatus` 找不到元素，状态从来不显示。

### 验证

从 `app.js` 抽取合并/去重片段跑 **10 项断言，全通过**：跨设备同内容只留一条、
保留本机那条、云端新增进得来、同图两种域名只留一条且保留可用地址、
空正文不误删、同文不同分类算两条、去重幂等。

---

## v1.15.1 · 2026-09-21

**主题：设置页开关「点了没反应」—— 视觉更新排在等写盘之后**

### 问题
设置页（尤其是云同步面板）的开关点下去要等一下才动，连点还会卡。

### 根因
全项目的设置函数都是同一个写法：

```js
cfg[k]=!cfg[k];
await saveAll();   // ← ① 等 IndexedDB 写盘（手机上几十~几百 ms）
syncUI();          // ← ② 全量 UI 同步，但视觉翻转藏在里面
```

**视觉更新被压在"等写盘"后面** —— 而写盘慢的时候，用户看到的就是"点了没反应"。

更浪费的是：`syncUI()` 有 136 行（遍历所有 `[data-img]`、几十个 `setSw`、多个 `querySelectorAll`），
但**云同步的三个开关压根不在 syncUI 里** —— 跑完一圈，一个云同步开关都没更新到。

### 修复：乐观更新

```js
cfg[k]=!cfg[k];
/* ① 立刻翻转被点的那个开关（当帧生效） */
setSw(被点元素, !!cfg[k]);
/* ② UI 同步与落盘挪到之后，不阻挡点击反馈 */
if(与全局 UI 相关) syncUI();
saveAllDebounced();   // ← 防抖：连点多个开关只落一次盘
```

| 改动 | 效果 |
| --- | --- |
| 视觉翻转前置 | 点下去**当帧**就有反馈 |
| 落盘改 `saveAllDebounced()` | 不再逐次 await；连点多个开关合并成一次写盘 |
| 云同步三开关跳过 `syncUI()`（`SYNC_SW_NO_UI` 名单） | 省掉 136 行的无用全量同步 |
| 元素从 `event` 取，不拼 `"sw_"+k` | 见下方顺带修复 |

### 顺带修复：两个「名不副实」的开关
`index.html` 里有两处 id 与配置名不一致（旧代码靠拼名字找元素，会找不到）：

| 开关 id | 配置名 |
| --- | --- |
| `sw_showSeconds` | `timeShowSeconds` |
| `sw_autoTTS_adv` | `autoTTS` |

现在统一从点击事件里取元素（`event.currentTarget`），命名不一致也不影响；
所有 `cfgToggle(...)` 调用点（`index.html` 27 处 + `app.js` 动态生成 6 处）都已补上 `event` 参数。

### 一并提速的其他设置项
同一个根因，一并改成乐观更新：`setLayout` / `setTheme` / `setUiFontSize` / `setChatFontSize` /
`setAvSize` / `setChatStyle`。

> 删除类操作（删卡片/音效/贴纸等）**保持** `await saveAll()` —— 那些是"确保删掉了再刷新列表"，
> 语义上就该等写盘完成，且不属于高频交互。`toggleNotif` 也保留 await（要等通知权限结果）。

### 安全性
这几项设置都不存在"写盘失败需要回滚 UI"的情况，所以乐观更新是安全的。
全仓已确认**没有任何地方 `await cfgToggle(...)` 或使用其返回值** —— 改成同步函数无副作用。

### 测试
`C:\Users\86176\AppData\Local\Temp\cy-moon-v1151-toggle-test.js`
—— 从真实 `app.js` 切片后跑 **21 项断言，全通过**：
- 翻转动作必须早于落盘（核心，4 项）
- 云同步开关跳过 `syncUI` + 普通开关仍调用（对照组，5 项）
- 名不副实的开关靠 `event` 也能翻转（2 项）
- 无事件对象时退回命名约定（兼容老调用，1 项）
- 静态检查：`cfgToggle`/`setLayout`/`setTheme`/`setChatStyle` 均已脱离 `await saveAll` 模式；
  两处文件的 `cfgToggle` 调用点全部带 `event`（6 项）

---

## v1.15.0 · 2026-09-21

**主题：聊天图片跨设备同步 —— 把「内嵌 base64」改成「按 id 独立寻址」**

### 问题
图片消息里那张图是**内嵌在消息自身的 `m.image`**（base64，400px/q0.65 ≈ 30–80KB）。
同步前必须剥媒体，于是 `delete c.image` → 对面拿到一条没有图的 `[图片消息]` 纯文本。

这不是一行代码能修的（画作可以，因为画作没有 base64、种子即内容）——
**图片是真的媒体本体，不解决"怎么传"就永远传不过去**。

### 方案：图片独立寻址

```
改前： chats[i].image = "data:image/jpeg;base64,……(30~80KB)"   ← 媒体与消息绑死
改后： chats[i].imgId = "ci…"                                  ← 消息只留短 id
       chatImgs["ci…"] = "data:image/jpeg;base64,……"           ← 图独立存放
```

这样图才成了**可单独寻址、单独传、单独回填**的资源。

### 🏗 数据结构变更（另一台设备拉到会出错的类型）

| 项 | 内容 |
| --- | --- |
| 新增 IndexedDB 键 | `chatImgs`（对象 `{imgId: base64}`，非数组） |
| 新增消息字段 | `imgId`；`m.image` **保留兼容**（渲染两种都认） |
| 迁移函数 | `_migrateChatsExtractImages()`（幂等） |

**四处加键**（本项目的经典踩坑位）：`dbGetBatch` / init 赋值 / `saveAll` / `fullExport` / `onPickJson`
—— 五处，全部已加；且已加入 `HEAVY_KEYS`（大且少变，正合增量写入机制）。

迁移幂等性：**消息里没有 `m.image` 就完全不动作**，所以跑第二次、或对纯文本/画作/表情消息都无影响。

### 同步实现

- **每张图一个文件** `chatimg-<id>.json`：绕开 `_syncParts` 只对数组分片的限制，
  以及 GitHub contents API 的 1MB 硬限。`manifest.files.chatImgs` 存文件名数组，拉取端反解 id。
- **只传被引用的图**（`_referencedChatImgs`）：消息删掉后对应的孤儿图不再上传。
- **单张 > 900KB 跳过并提示**：不让一张图把整次推送拖失败（压缩规格正常永远碰不到）。
- **`chatImgs` 永远并集，`force` 也不例外**：图是只增资源，覆盖只会丢掉本机独有的图（不可逆事故）。
  `force` 的语义是"对齐云端的文本/结构"，不是"删掉我的图"。
- **独立开关 `syncChatImgs`（默认关）**，不复用 `syncMedia`。

### 渲染兼容

`_msgKind`、`_buildMsgRow` 图片分支、引用缩略图、引用预览条、繁体转换排除条件
—— 全部同时认 `m.image`（老数据）和 `m.imgId`（新数据）。

图查不到时 `resolveChatImg` 返回 **`PH_SVG` 占位图**（可点开），
⛔ **不是退化成文字气泡** —— 占位图能看出"这里本来有张图"，比灰字有用。

聊天页脏检查签名（`_chatViewSigNow`）计入 `chatImgs` 规模 ——
否则"图到了但 chats 数组没变"时切进聊天页会复用旧 DOM，图还停在占位图上。

### 测试
`C:\Users\86176\AppData\Local\Temp\cy-moon-v1150-img-test.js`，从真实 app.js 切片后跑
**44 项断言，全通过**：画作回归 4 · 迁移 7 · 渲染兼容 4 · 合并回填 7 · 同步包 7 · 静态检查 15。

### ⚠ 使用须知

1. **两台设备都升到 1.15.0 之后再打开开关** —— 旧版（1.14.x）拉到 `imgId` 消息会认不出类型。
2. **首次打开开关会一次性上传全部历史图片**，可能产生一次较大的云端提交；之后只传新增的。
3. **应急备份（localStorage）不包含 `chatImgs`** —— 恢复后图片显示占位图（刻意取舍：只保证文字不丢）。
   完整图片请用「数据 → 导出备份」（走 `fullExport`，含 `chatImgs`）。
4. 导入**老备份**（图还内嵌在 `m.image`）时会自动跑一次迁移，把图挪进 `chatImgs`。

---

## v1.14.2 · 2026-09-21

**主题：合并拉取的「单向回填」—— 治 `[画作]` 灰字气泡在对面永久定格**

### 问题
v1.14.1 修好了「**新发的**画作推送时被剥掉种子」，但用户实测**新发的画作跨设备后仍是 `[画作]`**。
（发送端正常、只有另一台设备显示成灰字气泡。）

### 根因
v1.14.0 引入的「合并式拉取」里，`_mergeByKey` 是**同键无条件跳过云端**：

```js
if(k!=null){ if(seen.has(k)) continue; seen.add(k); }   // ← 同 mid 直接 continue
```

这条规则本意是「防旧数据覆盖本机」，方向对；但它顺带堵死了另一件事：
**当本机那条消息恰好是残缺版（被剥过媒体）时，云端那份完整版永远进不来。**

于是形成死锁：

1. 某台设备在 v1.14.1 之前拉取过一次 → 落下一条 `{mid, text:"[画作]"}`（没有 painter）
2. 之后无论怎么点「合并云端数据」，云端那条完整的 `{mid, painter:true, painterSeed:"4278193"}`
   都因为 mid 撞车被 `continue` 跳过
3. 结果：这台设备上的画作**永久**停在 `[画作]` 灰字气泡——**新发的也一样**，
   因为「新发」只保证云端有完整版，不保证本机能收下它

### 修复
给 `_mergeChats` 加一段**单向回填**（只作用于聊天记录，不动 `_mergeByKey` 本身，避免波及 cards/members/surveys 等其他列表类）：

| 本机缺什么 | 云端有 | 结果 |
| --- | --- | --- |
| 无 `painter` | `painter:true` + `painterSeed` | 补上 → 能重绘 |
| 无 `image` 且无 `imgId` | `image` 或 `imgId` | 补上 → 能显示 |
| 无 `sticker` | `sticker:true` + `stickerId` | 补上 → 引用行不再退化 |

**方向严格单向**：只允许「云端补齐本机**缺**的媒体字段」。
⛔ **绝不改写** `text` / `ts` / `sender` / `name` —— 本机的新文本、新时间戳仍然优先，
「防旧数据覆盖」的初衷一点没丢。

### 附带
`_msgKind` 的图片判定由 `if(m.image)` 放宽为 `if(m.image || m.imgId)`，
为 v1.15.0 的图片独立寻址（图存 `chatImgs`、消息只留 `imgId`）提前铺路。

### 测试
从真实 `app.js` 切片 `_mergeByKey` / `_keyBy` / `_mergeChats` 组装后跑 **21 项断言，全通过**：
画作回填 4 · 图片回填 2 · 「本机优先」回归 5 · 表情包 2 · 并集排序 3 · 脏数据边界 3 · `_mergeByKey` 未被污染 2。

---

## v1.14.1 · 2026-09-21

**主题：画作消息云同步后变成 `[画作]` 纯文本气泡 —— 剥媒体时误删了种子**

### 问题
手机与电脑之间同步后，聊天里的**画作**全部退化成 `[画作]` 文字气泡，看不到图形。

### 根因
`_stripChatMedia`（同步前剥媒体的函数）里有这么一行：

```js
if(c.painter){ c.text=c.text||"[画作消息]"; delete c.painter; delete c.painterSeed; }
```

画作消息的形态是 `{painter:true, painterSeed:"4820193"}` —— **一个 base64 都没有**。
渲染靠 `cfg.painterUrl` + `?seed=<painterSeed>` 现场重绘（见 `_buildMsgRow`）。

于是剥掉 `painter` / `painterSeed` 之后：
- `_msgKind` 认不出它是画作 → 判定为 `text`
- 渲染落到文本气泡分支 → 显示 `[画作消息]`

**画作本来是全部消息类型里最容易跨设备的**（种子即内容，任何设备都能画出同一张图），
却被这行砸了。删它省不到几十字节，只砸了功能 —— 与之前 `stickerId` 那个错误完全同源。

### 修复
- `_stripChatMedia`：**保留 `painter` / `painterSeed`**，只兜底 `text`
- `_sanitizeMsgsForBackup`（本地应急备份）：同样改为保留
- 渲染层无需任何改动 —— 字段在了，`_msgKind` 自然认得出

### 为什么不需要开关
实测同一份数据**剥离前后 JSON 长度都是 99B，净增 0**。
`painterSeed` 只是个 7 位数字符串，和不带它相比没有体积差 —— 加开关只会让人困惑
"为什么画作也要开关"。所以画作**始终同步**。

### 测试
`C:\Users\86176\AppData\Local\Temp\cy-moon-v1141-painter-test.js` —— 从 `app.js` 切片真实代码跑，
**18 项断言全绿**：painter/painterSeed 保留 / `_msgKind` 仍判 painter / 备份同样保留 /
体积对照（净增 0B）/ 边界（无 seed 不抛错）/
既有修复未回退（sticker 锚点保留、图片与头像仍被剥离、纯文本原样通过）/
**修复前后对照（修复前确实退化为 text）**。

### 遗留（下一步 v1.15.0）
**图片消息**同样退化成 `[图片]`，但它是结构性问题 —— 图片的 base64 存在消息自身的
`m.image` 字段里（400px/q0.65 ≈ 30–80KB），不搬进同步包就不可能显示。
计划：新增 `chatImgs` 键做独立寻址（消息只留 `imgId` 引用），配一个默认关闭的
「同步聊天图片」开关，每张图独立成文件以绕开 1MB 限制。
⚠ 另一处必须一起改：`_mergeByKey` 同键**无条件跳过**云端，导致某设备一旦落了「残废版」
消息，之后无论怎么合并云端完整版都进不来 —— 需要给 `_mergeChats` 加"单向回填"特例。

---

## v1.14.0 · 2026-09-21

**主题：云同步从「整包覆盖」改成「合并式拉取」—— 两个设备的内容不再互相盖掉**

### 问题
原来的 `_syncApply` 是**整包覆盖**：拿到云端包就 `chats = b.chats`、`msgs = b.msgs`
这样直接赋值，本机那份原样丢弃。后果必然出现"手机盖电脑 / 电脑盖手机"，
**点错一次就是数据事故**（v1.13.1 修的 409 死锁正是它的产物）。

### 改法：拉取默认走合并
- **列表类按唯一键取并集**，同键**以本机为准**（本机那份通常更完整，比如带头像）：
  | 数据 | 唯一键 |
  |---|---|
  | 聊天记录 `chats` | `mid` |
  | 留言板帖子 `msgs` | `id` |
  | 帖子评论 `msgs[].comments` | `id`（**递归并集**） |
  | 卡片 `cards` / 表情包 `stickers` / 群成员 `groupMembers` | `id` |
  | 问卷 `surveys` / 答卷 `surveyRecords` | `id` |
  | 轮播 `carousel` / 音效 `sounds` | `id` |
  | 屏蔽分类 `shieldedCats` / 折叠分类 `foldedCats` | 字符串 Set 并集 |
  （以上唯一键已逐项核对过生成函数，两端稳定且一致。）
- **单条配置类无法合并，按时戳裁决**：`texts` / `cfg` / `anniversaries`。
  规则：**云端 manifest 的 `ts` 比本机 `syncLastPush` 新 → 用云端**，否则保留本机。
- 留言板支持「同帖评论并集」——两端各回了同一条帖子一条评论，也不会互相覆盖。
- 聊天合并后**按 `ts` 排回时间序**，并遵守 `CHAT_MAX` 上限（只留最近那批）。

### 两个覆盖出口（对称）
- `forcePushSync()`（v1.13.1 已有）· **强制推送 · 本机→云端**
- `forcePullSync()`（本次新增）· **强制拉取 · 云端→本机**，`pullSync(false, true)`
- 合并拉取只做加法，**表达不出"删除"**（云端删的、本机想丢的）。要彻底对齐某一边才用覆盖按钮，
  两个都带二次确认。

### 顺带：同步出去的设置不再带本机同步状态
`_syncCfgOut()` 原先只删令牌，现在**连同 `syncShas` / `syncLastPush` / `syncLastPull` 一起删**。
这几项是**本机私有**的 —— 同步出去等于让另一台设备的 sha 表和时间戳覆盖过来，
那是 sha 错乱、推送莫名 409 的另一个来源。

### 面板文案
云同步面板按钮改为两排：`测试连接 / 推送到云端 / 合并云端数据` +
`强制推送 · 本机→云端 / 强制拉取 · 云端→本机`，并注明合并"不会删本机任何内容、
但云端删除体现不出来"。启动检测的确认框也由"拉取覆盖本机"改为"合并到本机"。

---

## v1.13.1 · 2026-09-21

**主题：云同步的「版本冲突」原本是单向死锁 —— 补一个强制推送出口**

### 问题
推送用的是**本机缓存的分片 sha**（`_ghPutFile` 只在缓存为空时才去 GET 最新 sha）。
所以别的设备推过之后，本机再推必然 409。而原来的提示是：

> 云端已被其他设备改过，请先「拉取」再推送

**这句话在"本机才是最新"的情况下是害人的** ——「拉取」会跑 `_syncApply`，
把云端那份**直接盖到本机数据上**（`chats=b.chats` 这种直接赋值）。
若另一台设备误推了一份测试数据，照做就等于把自己的数据毁掉，而且没有反向出口。

### 修复
- 新增 `forcePushSync()` + 云同步面板「强制推送」按钮：
  清空 `cfg.syncShas`，让 `_ghPutFile` 逐个重新 GET 最新 sha 再 PUT ——
  用**本机数据覆盖云端**。带二次确认（云端那份会被覆盖且不可恢复）。
- 409 提示改为分辨方向：
  「本机数据若是最新，用「强制推送」覆盖云端；云端若是最新，才用「从云端拉取」」
- 面板补一行说明，把两个按钮的方向讲清：
  **拉取 = 云端覆盖本机；强制推送 = 本机覆盖云端。**

### 顺带
清理云端残留分片不是必须的：拉取只按 `index.json` 里的 `manifest.files` 读文件，
不会被旧的 `xxx-p7.json` 之类干扰（它们只是占空间）。

---

## v1.13.0 · 2026-09-21

**主题：手机端"越用越慢 / 卡"的三处根治**

三个症状：越用越慢、进设置页调设置卡、进聊天页卡。
共同点：**都是页面侧的开销，与手机壳子无关**（壳子是第三方打包的，改不了配置，也不该换 ——
WebView 私有目录不被系统清理正是套壳的目的）。

### ① 聊天记录也走增量写入
- `HEAVY_KEYS` 补入 `chats` / `members`。
  它们不是"大"而是"会越用越大"：设置页每个开关都是 `await saveAll(); syncUI();`，
  聊天攒到 2000 条时，每次改设置都要把整个聊天记录重新序列化落盘。
- `_cheapSig` 对**长数组**改为采样：首 4 / 1/4 / 中 / 3/4 / 尾 4，共 20 个点。
  原先对数组是**全量递归**，2000 条要逐条算 —— 实测 5.6ms，采样后 0.08ms（约 70×）。
- 为什么采样安全：全项目**没有"编辑单条消息"的功能**，改动必然表现为
  length 变化（增删）或首 / 中 / 尾变化（追加、整体替换、超上限截断）。已用 10 个用例验证。
- ⚠ **`msgs`（留言板）有意不进名单**：`checkBoardUnread` 会**原地**把单条帖子的 `read`
  改成 true，而长数组签名是采样的 —— 改在非采样点上就抓不到，后果是"已读状态不落盘"。
  对照测试（F10）确认了这一点。要收它得给原地修改加脏标记，留给后续。
- `_cheapSig` 深度上限 4 → 8（该上限只用于兜住循环引用）。

### ② 设置页的图片指纹
- `syncUI()` 的 `[data-img]` 注入改为"指纹没变就不碰 `src`"。
  给 `<img>.src` 赋上同一个 base64 字符串，浏览器也会把整串重新解析、重新解码；
  而 `syncUI()` 最高频的触发者正是"改设置"，设置页又有几十个 `[data-img]`。

### ③ 进聊天页的脏检查
- `closeApp` 只是 `classList.remove("active")`，DOM 一条都没删；
  但 `openApp("chatApp")` 每次都 `renderChats()`（先 `innerHTML=""` 再重建 150 条）。
- 新增 `_chatViewSig` / `chatViewIsFresh()`：特征一致且 DOM 非空就复用。
  **方向保守** —— 签名过时只会多重建一次（等同旧行为），
  绝不在 DOM 未更新时刷新签名（那会显示陈旧内容）。
- 附带收益：在聊天里翻过的历史位置，切走再回来不会被打回最近 150 条。

### 验证
`node --check` 通过；29 项断言全绿，覆盖签名碰撞、saveAll 跳过决策、
视图签名、以及"该写的键一个都没漏"（F1-F10）。

---

## v1.12.1 · 2026-09-21

**主题：身份资料（头像 / 昵称）变更后的视图同步**

### 修复
- **换头像后聊天里还挂着旧头像**：消息行（`_buildMsgRow`）和留言板的头像 / 昵称是**直接写进
  `src` / `innerHTML` 的**，没有 `data-img` 属性 —— 所以 `syncUI()` 那套"按 `imgs` 注入"的机制
  碰不到它们，改完头像必须退出再进一次聊天页才会刷新。
- 新增 `refreshIdentityViews()`，在以下身份变更点调用，立即重渲染聊天与留言板：
  - `onPickImg` 的 `__memberAvatar__` 分支（群成员头像）
  - `onPickImg` 的 `selfAvatar` / `oppAvatar` 分支（全局头像）
  - `renderMembers` 里的改名 / 剔除成员
  - `clearImgKey` 清除 `selfAvatar` / `oppAvatar` 时
- 不改数据结构，无需迁移。

### 文档修正
- 头像压缩规格的注释与更新记录曾写成 **160px**，代码实际是 **240px / q0.72**（`AVATAR_MAX_W`）。
  已统一为 240px —— 两者都够清晰，但文档与代码不一致会误导后续排查。

---

## v1.12.0 · 2026-09-21

**主题：手机端存储治理（头像压缩 / 引用定位 / 写入放大）**

### 头像专项压缩
- 头像不再和背景图共用 `_compressImg(f, 1200, 0.75)`（一张 200–400KB），
  改走新增的 `_compressAvatar`：**240px / q0.72**，并统一铺白底后重编码（PNG 透明区不会变黑）。
  头像在界面里最大只渲染到 ~56px，240px 是 4 倍以上超采样，足够清晰。
- 新增 `_migrateSqueezeImages()`：老数据一次性重压（幂等，受 `cfg.imgSqueeze` 版本门槛保护）。
  覆盖 `imgs.selfAvatar` / `imgs.oppAvatar` / `groupMembers[].avatar` / `carousel[].data` / `imgs.mosaic[]`。
  **背景图（`chatBg` / `homeBg` / `aes_body_bg`）不动** —— 全屏铺底，压狠了会糊。
  只在结果确实更小时才替换。
- 轮播 1200px → 1080px/q0.72；马赛克格子 1200px → 400px/q0.65（按实际显示尺寸给）。

### 引用定位（图片 / 表情包）
- `_stripChatMedia` / `_sanitizeMsgsForBackup` **不再删除 `sticker` / `stickerId`**。
  这两个字段只是引用锚点（布尔 + 短 id，几十字节），真正的 base64 在 `stickers` 数组里，与消息无关。
  删掉它们会让 `_msgKind` 认不出表情消息 → 引用行退化成纯文本，连"引用的是哪个表情"都定位不到。
- 引用行缩略图带 `data-view`：**点缩略图直接看原图**，点其余区域才跳到原消息。
- `quoteLineHtml` 的文本兜底（`data-jump`）只对纯文本 / 歌词类引用生效。
  图片、表情包消息的 `text` 都是同一个占位串（`[图片]` / `[表情包]`），
  一旦 `mid` 失效，按文本找会跳到**第一条**同类消息上 —— 宁可什么都不跳，也不跳错。
- `makeQuote` 在 `text` 为空时用 `QUOTE_LABEL[kind]` 兜底，原消息被裁剪后引用行不会变空白。

### 写入放大
- `saveAll` 改为**增量写入**：`imgs` / `carousel` / `sounds` / `stickers` 这 4 个"大且很少变"的键，
  签名没变就整块跳过。签名用新增的 `_cheapSig`，只读 `length` + 首尾各 24 字符，
  **成本与字节数无关**。原来每发一条消息（300ms 防抖）都会把 `imgs`（实测 2.4MB）重新落盘。
- 写失败时清空签名表，避免这些键被当成"已写过"而永久跳过（等于静默丢数据）。

### 其他
- `backupCriticalData`（localStorage 应急备份）多一层降级：超配额时再去掉群成员头像重试。
- 新增 `window.showVersionInfo()`：数据 → 关于 · 版本，显示当前版本与更新记录。
- 启动时 `console.log` 输出版本 / DB 版本 / 聊天条数，方便排查。

---

## v1.11.0 · 2026-09-21

- 聊天消息不再内嵌头像 base64。实测云端 chats 分片 4.0MB / 193 条，
  其中 **12 条群聊消息各带一份 330KB 头像，占 99.6%**；
  而渲染早就改成"有 `memberId` 就查 `groupMembers`"，消息里那份是永不读取的死数据。
- 新增 `_migrateChatsDropAvatar()`（老数据无 `memberId` 时先按 `name` 补齐再删，幂等）；
  `_addChatMsg` 落地前 `delete msg.avatar`（构造处的 avatar 保留，继续服务通知 / 弹窗）。
- 实测：3996KB → 36KB，**降 99.1%**；幂等复跑不变。

## v1.10.0 · 2026-09-21

- **安全**：`fullExport` 改用 `_syncCfgOut()`（原本直接把整个 `cfg` 写进备份文件，含明文 `syncToken`）；
  `onPickJson` 补上 `{syncToken: cfg.syncToken}`，导入不再覆盖本机令牌。
- 留言板楼层顺序错乱修复：统一双时间轴（`_cmtRealTs` / `_commentsOf`），
  `sendBoardComment` 入库前排序，`_lastTsIn` 改取 `Math.max(ts)`。
- 组字接入三套云端语料（成语词典 / 汉字表 / 歇后语）；`_markovSentence` 门槛同步算上词典
  （只改 `genRecomb` 会导致静默退化成模板句）。

## v1.9.0 · 2026-09-20

- 新增云同步：以 GitHub 私有仓库作后端（无服务器），支持推送 / 拉取 / 连通性测试。
  默认**只同步文本与元数据**：`imgs` / `sounds` / `carousel` 是 base64（几 MB），
  既撞 API 1MB 限制也会撑爆提交历史，由 `syncMedia` 开关控制；聊天里的图片走占位文本。
  令牌只存 `localStorage(cfg.syncToken)`，**绝不硬编码**。
- 409 冲突不静默覆盖；启动时云端更新只 `confirm` 提示，绝不自动覆盖本地。

## v1.8.0 · 2026-09-20

- 留言板重构为「帖子 + 评论」两层（新增 `normalizeMsgs` 迁移）。
- 组字数字槽位与句首虚词；模板槽位扩充。
- 电脑端限宽布局。
- 歌词元数据过滤修复（单字职务名 + 英文关键词）。
- 修复 `stickers` 备份链路。

## v1.7.0 · 2026-08-10

- 接入画作 `painter` 页面（`iframe` 嵌入远端 cy-painter，透传 seed）。
- 样式与组件细化。（此版本及更早的提交信息较粗，明细以 git 记录为准）

## v1.6.0 · 2026-07-14

- 头像短按不再误触发上传弹窗；`openApp` 会先关掉残留浮层。
- 切换聊天布局后同步单选、滚动并聚焦输入框。

## v1.5.0 · 2026-07-12

- 响应式改造：安全区 + 多断点设备适配 + 组件尺寸细化。
- 聊天分页、自动回复概率、表情包压缩、完整备份 / 恢复、虚拟时钟。
- 跳转到指定消息、歌词显示增强、云曲库浏览。
- 云字卡库、云表情库、字卡 JSON 导入导出。
- 存储管理、防抖搜索、Android PWA meta。

## v1.0.0 · 2026-07-03

- 初版发布（幸逢页面，GitHub Pages 经典部署）。
